这个文档记录我在Python项目练习中学到的知识
1、argparse模块可以用于解析python 运行时输入的参数；
2、PIL（pillow）库可以对图片作基本处理；
3、PIL的Image库中，getpixel函数的参数是元组形式，所以参数里要多加一个括号；
4、函数的参数列表里有*，表示解析参数，可以把元组参数变成可变参数，很有用；
5、with open as 语句用于文件的读写操作很方便；

